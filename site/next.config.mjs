/** @type {import('next').NextConfig} */

// ---------------------------------------------------------------------------
// ЕДИНСТВЕННОЕ, ЧТО НУЖНО ПРАВИТЬ РУКАМИ:
//
// GitHub Pages отдаёт обычный репозиторий по адресу
//   https://<логин>.github.io/<имя-репозитория>/
// В этом случае сюда пишем '/<имя-репозитория>' — иначе стили, картинки
// и видео не найдутся и страница откроется голым текстом.
//
// Оставить пустым '' нужно только в двух случаях:
//   — репозиторий называется <логин>.github.io
//   — подключён собственный домен (sonyascandal.com)
// ---------------------------------------------------------------------------
const basePath = '/skandal'

const nextConfig = {
  // Статический экспорт. next build кладёт готовый HTML в папку ./out
  output: 'export',

  basePath,

  // /impressum превращается в /impressum/index.html — самая надёжная форма
  // для GitHub Pages и любого другого статического хостинга.
  trailingSlash: true,

  // Обязательно при экспорте: сервера, который оптимизирует картинки, нет.
  images: {
    unoptimized: true,
  },

  typescript: {
    ignoreBuildErrors: true,
  },

  // Блока async headers() здесь больше нет намеренно: он требует запущенный
  // Next.js-сервер и несовместим с output: 'export' — сборка с ним падает.
  // Заголовки безопасности для статики лежат в public/_headers (работают на
  // Cloudflare Pages и Netlify; GitHub Pages кастомные заголовки не умеет).
}

export default nextConfig
